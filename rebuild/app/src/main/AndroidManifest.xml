<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <queries><package android:name="com.mojang.minecraftpe" /></queries>
    <application
        android:name=".FoundationApplication"
        android:allowBackup="false"
        android:label="MATH Foundation"
        android:icon="@drawable/ic_math"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foundation">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProbeActivity"
            android:exported="false"
            android:process=":probe"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>
    </application>
</manifest>
