package com.google.androidgamesdk;

import android.app.Activity;

/**
 * Stage 3.8 only needs the exact superclass identity while JNI registrations are tested.
 * This shell is never declared in the manifest and is never instantiated.
 */
public class GameActivity extends Activity {
}
